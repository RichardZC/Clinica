
@model List<BE.articulo>

<div id="email-list" class="col s12 m12 l12 card-panel z-depth-1">
    <ul class="collection">
        @{ int i = 0;}
        @foreach (var item in Model)
            {
                i++;
            <li class="collection-item avatar @if (i == 1) {<text>selected</text>}"
                ondblclick="MostrarEditar(@item.ArticuloId);"
                onclick="$(this).addClass('selected').siblings().removeClass('selected');">
                <span class="circle light-blue">@item.Denominacion.Substring(0, 1)</span>
                <span class="email-title">@item.Denominacion</span>
                <p class="truncate grey-text ultra-small">@item.Codigo</p>
                <a class="secondary-content email-time"><span class="blue-text ultra-small">DNI @item.Codigo</span></a>
            </li>
        }
    </ul>

    <div class="fixed-action-btn" style="bottom: 50px; right: 19px;">
        <a class="btn btn-floating btn-large red" href='@Url.Action("Mantener","Articulo",new { id=0 })'>
            <i class="mdi-social-person-add"></i>
        </a>
    </div>
</div>
