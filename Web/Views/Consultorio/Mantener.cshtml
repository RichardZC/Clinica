<main>
    <div id="basic-form" class="section">
        <div class="row">
            <div class="col s12 m12 l6">
                <div class="card-panel">
                    <h4 class="header2">Mantener Consultorio</h4>
                    <div class="row">
                        <form id="formValidate" class="validar" method="post">
                            <input type="hidden" v-model="consultorio.ConsultorioId" />
                            <div class="row">
                                <div class="input-field col s12">
                                    <input v-model="consultorio.denominacion" type="text" required>
                                    <label for="first_name">Nombre Consultorio</label>
                                </div>
                            </div>
                            <div class="input-field col s12 m3 l12">
                                <button v-on:click="guardarConsultorio" class="btn waves-effect waves-light right">
                                    Guardar
                                    <i class="mdi-action-done right"></i>
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>
@section scripts{
    <script>

        var vconsultorio = new Vue({
            el: 'main',
            data: {
                consultorio: {}
            },

            methods: {
                guardarConsultorio: function () {
                    if (this.consultorio.ConsultorioId > 0) {
                    }
                    else {
                        this.consultorio.ConsultorioId = 0;
                    }

                    axios.post('/clinica/Consultorio/GuardarConsultorio', {
                        consul: this.consultorio,
                        href: "",
                    })
                  .then(response => {
                      console.log(response);
                  })
                  .catch(error => {
                      console.log(error);
                  });
                }
            }

        })



    </script>
}
